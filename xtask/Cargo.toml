[package]
name = "xtask"
version = "0.43.0"
edition = "2024"
publish = false

[dependencies]
mirx = { path = "../mirx" }
mirui = { path = "..", default-features = false, features = ["std"] }
serde_json = "1"
